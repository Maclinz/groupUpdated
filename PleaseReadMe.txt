To run the project: Node js must the intalled below is a link to download it -:
https://nodejs.org/en/download/

After Node js is installed then open the project folder in a desired code editor.

1. Run: 'npm install dependencies' inside the project folder using the terminal
2. Run: 'npm start' after the dependencies are installed